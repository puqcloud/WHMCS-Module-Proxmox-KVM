# Cron and Automation

How the module's cron-driven state machines work: the deploy pipeline that walks a new VM from creation to ready, the change package pipeline for upgrades and downgrades, and all other scheduled tasks (snapshot cleanup, backup schedule, stats collection) with their intervals and locking behaviour.
